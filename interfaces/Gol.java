package one.digitalinnovation.classes.interfaces;

//implements to use an interface
public class Gol implements Carro{

    //setting the marca method, ligar() is already in (default)
    @Override
    public String marca() {return "Volkswagen";}
}
